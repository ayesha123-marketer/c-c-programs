#include <iostream>
#include <fstream>
#include <string>
using namespace std;

int main()
{
    int choice;
    string task;

    cout << "===== TO-DO LIST =====\n";
    cout << "1. Add Task\n";
    cout << "2. View Tasks\n";
    cout << "Choice: ";
    cin >> choice;

    cin.ignore();

    if(choice == 1)
    {
        cout << "Enter Task: ";
        getline(cin, task);

        ofstream file("tasks.txt", ios::app);
        file << task << endl;
        file.close();

        cout << "\nTask Added!\n";
    }
    else if(choice == 2)
    {
        ifstream file("tasks.txt");

        while(getline(file, task))
        {
            cout << "- " << task << endl;
        }

        file.close();
    }

    return 0;
}
