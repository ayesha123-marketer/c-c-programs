#include <iostream>
#include <fstream>
#include <string>
using namespace std;

int main()
{
    int choice;
    string note;

    cout << "===== NOTES SAVER =====\n";
    cout << "1. Save Note\n";
    cout << "2. View Notes\n";
    cout << "Choice: ";
    cin >> choice;

    cin.ignore();

    if(choice == 1)
    {
        cout << "Write Note: ";
        getline(cin, note);

        ofstream file("notes.txt", ios::app);
        file << note << endl;
        file.close();

        cout << "\nNote Saved!\n";
    }
    else if(choice == 2)
    {
        ifstream file("notes.txt");

        while(getline(file, note))
        {
            cout << note << endl;
        }

        file.close();
    }

    return 0;
}
